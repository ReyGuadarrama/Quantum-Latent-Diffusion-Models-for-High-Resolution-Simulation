In this section, we present the Denoising Diffusion Probabilistic Model (DDPM). As an initial experiment, the model was trained on a reduced dataset of 10,000 jet images for only 5 epochs.

This repository contains two main programs:
 - The final version: The implementation that achieved the best results.
 - A simplified DDPM: Used specifically for the quantum experiments (or quantum tests).
